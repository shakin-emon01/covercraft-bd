{
  "name": "covercraft-bd",
  "version": "1.0.0",
  "description": "Academic cover generator for Bangladeshi universities",
  "scripts": {
    "dev": "pnpm -r --parallel --filter web --filter server run dev",
    "dev:web": "pnpm --filter web run dev",
    "dev:server": "pnpm --filter server run dev",
    "build": "pnpm --filter web run build && pnpm --filter server run build",
    "seed": "pnpm --filter server run seed",
    "typecheck": "pnpm --filter web exec tsc --noEmit -p tsconfig.app.json && pnpm --filter server exec tsc --noEmit -p tsconfig.json"
  },
  "pnpm": {
    "overrides": {
      "vite": "^8.0.0-beta.13"
    }
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.3"
}
